import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Provider Report — OpenMedicaid",
};

export default function ReportLayout({ children }: { children: React.ReactNode }) {
  return children;
}
